# agent.md

## 默认输出语言

- 默认输出语言为中文（简体）。
- 与用户的对话、进度更新、报错说明默认使用中文。
- 生成或修改 wiki 内容时，正文、标题、字段说明默认使用中文。
- 公司名、ticker、产品名、技术术语等专有名词保留原文。
- 只有当用户明确要求英文输出或明确要求英文模板时，才切换到英文。

## 作用范围

- 本文件作为通用 agent 入口约定，补充说明仓库默认语言行为。
- 更完整的 wiki 协议、摄入规则、交叉引用、矛盾扫描与结晶化规则，以 `CLAUDE.md` 和 `wiki/_protocols.md` 为准。
